; Local dev only — keeps PHP 8.5 deprecation notices from being written into
; HTTP response bodies (they leak because Laravel's LoadConfiguration bootstrapper
; runs before HandleExceptions sets display_errors=Off).
; Loaded via PHP_INI_SCAN_DIR when starting the dev servers; not global.
display_errors = Off
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
