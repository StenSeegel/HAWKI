; PHP-Konfiguration für HAWKI Transkription
; Diese Datei wird beim Start von "php artisan serve" geladen
; Für andere Webserver: siehe .htaccess oder Webserver-Konfiguration

memory_limit = 512M
max_execution_time = 900
max_input_time = 900
upload_max_filesize = 100M
post_max_size = 100M
